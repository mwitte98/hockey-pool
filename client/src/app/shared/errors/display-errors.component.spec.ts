import { DisplayErrorsComponent } from './display-errors.component';

describe('DisplayErrorsComponent', () => {
  let comp: DisplayErrorsComponent;

  beforeEach(() => {
    comp = new DisplayErrorsComponent();
  });

  it('component is created', () => {
    expect(comp).toBeTruthy();
  });
});
