import { EntrySubmittedDialogComponent } from './entry-submitted-dialog.component';

describe('EntrySubmittedDialogComponent', () => {
  let comp: EntrySubmittedDialogComponent;

  beforeEach(() => {
    comp = new EntrySubmittedDialogComponent();
  });

  it('component is created', () => {
    expect(comp).toBeTruthy();
  });
});
